// For the sake of convenience, the datasets are global objects.

let labels = Labels()
let trainingDataset = SoundDataset(split: .train)
let testingDataset = SoundDataset(split: .test)
//let settings = Settings()
//let history = History.load()
